sprint: scd2-per-version-renderings
parent_branch: prepare_for_classroom_env
baseline_sha: d52337e5bec96660001735138aec0279cb54fedb
started: 2026-08-21
current_phase: 1
capabilities:
  - dimensional-exporter: [scd2 per-version value renderings]
  - validation-runner: [Scd2ColumnModeSupported widened, Scd2DerivedSourceConstant deleted]
gates:
  tests:
    - "make test"
phases:
  1:
    status: pending
    name: "Per-version value renderings on type2"
    demo: "docs/sprints/scd2-per-version-renderings/demos/phase_1_per_version_renderings.py"
    steps:
      - kind: source
        summary: "Widen the type2 mode gate, delete the constant-source gate, reshape the five per-row builders to source_expr, compile tracked derived sources per version (cast-then-authority); create the demo"
      - kind: migrate
        tactic: fan-out
        change: "The five pure per-row builders take source_expr (a qualified SQL expression) instead of grain_alias; build_date_parse_expr/build_decimal_expr/build_json_precision_expr take table_label instead of table_decl; all defaults dropped"
        files:
          - "tests/exporters/dimensional/test_columns.py"
          - "tests/exporters/dimensional/test_scd.py"
      - kind: author
        summary: "Rewrite the Scd2DerivedSourceConstant test block (deleted validator; tracked sources now legal, slice-only refusal moves to the slice-only surface) and move decimal/json_precision to the supported-mode params"
        files:
          - "tests/exporters/dimensional/test_validation.py"
          - "tests/exporters/dimensional/test_scd2_source_filter.py"
      - kind: author
        summary: "Author the new per-version rendering suite (all five renderings over tracked sources, election-invariant version structure, historical-value guards, source-class-blind and windowed cases)"
        files:
          - "tests/exporters/dimensional/test_scd2_renderings.py"
