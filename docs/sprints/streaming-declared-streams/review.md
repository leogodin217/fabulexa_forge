# Sprint Review: streaming-declared-streams

**Date:** 2026-08-11
**Reviewer:** Claude (fresh eyes, tier-2 context loaded, re-review after cleanup commit)

## Summary
| Gate | Severity | Findings | Notes |
|---|---|---|---|
| 1. Dead code scan | Clean | 0 | No `# TODO`/`# FIXME`/removal-comment markers added anywhere in the sprint diff (incl. cleanup commit); no bare `pass` bodies; the three retired private helper pairs (`_known_records_kinds`, `_kind_reference_targets`/`_kind_stream_reference_targets`, `_membership_reference_fields*`) were fully deleted, not aliased — `find_workspace_symbols`/grep confirm zero remaining references to the old names. |
| 2. Consistency / DRY (Gate-2 fix) | Clean | 0 | `known_records_kinds`, `kind_reference_targets`, `membership_reference_fields` now live once in `src/fabulexa_forge/exporters/streaming/routing.py`; `find_references` confirms both `engine.py` and `init.py` import and call the shared versions (no local reimplementations remain). `known_records_kinds` now reads `TableSpec.category == "records"` / `TableSpec.record_kind` (matching `exporters/source/plan.py` and `exporters/source/init.py`'s existing convention) instead of string-slicing `records__<kind>` table names — the previous implementation-drift is resolved. No new duplication introduced; `engine.py`'s two call sites still each locally compute `frozenset(known_records_kinds(sidecar))` per gate call (pre-existing pattern, unchanged by the fix, not a new issue). |
| 3. Test name audit | Clean | 0 | New tests (`test_debezium_value_schema_follows_elect_after_image_columns`, `test_reference_renders_sub_typed_target_record_index`, `test_sub_typed_owner_elects_uniform_surface_across_its_domain`, `test_keys_membership_member_field_reference_degrades_admitted_kind`) each assert exactly what their names claim. |
| 4. Test value audit | Clean | 0 | New tests assert exact values (`after_columns == ["record_index", "presentation_id", "prop__status"]`, `after["prop__pet_id"] == "0"`, `after["record_index"] == "0"`/`"1"`, an exact commented-YAML substring) — no laxity (`len(x) > 0`, bare `is not None`) against known fixture values. |
| 5. Coverage analysis (Gate-5 fix) | Clean | 0 | All four previously-flagged branches now hit by real, independently-verified passing tests: (a) `TestSubTypedMembershipOwner` exercises the sub-typed-owner branch of `_resolve_membership_stream_surface` via a new `membership__creature__toys` fixture table (creature is genuinely sub-typed: cat/dog); (b) `test_reference_renders_sub_typed_target_record_index` exercises `_resolve_target_identity`'s sub-typed-target translation on a positive (non-raising) path; (c) `test_debezium_value_schema_follows_elect_after_image_columns` drives `elect_after_image_columns`'s `record_index` branch through the actual Debezium schema-building path (not just `_rekey_after_image`); (d) `test_keys_membership_member_field_reference_degrades_admitted_kind` forces the membership member-field loop of `_self_gate_streaming_keys` (a new isolated fixture with no kind-shaped reference at all, so only the membership loop can trigger the degrade). Coverage confirms: `engine.py` 100%, `init.py` 99% (`tests/exporters/streaming/` + `tests/config/test_stream_config.py`) — the 3 remaining `init.py` misses (142-143, 424) are pre-existing, unrelated memoization/fallback branches never flagged by either review; trivial, not blocking. None of the four fixes read as a weakened/loosened pre-existing test — all four are net-new test functions plus net-new fixture data. |
| 6. Type-ignore density | Clean | 0 | Sprint-wide diff carries 2 `# type: ignore[arg-type]` lines in `engine.py` (pre-existing from Phase 2/3, untouched by the cleanup commit) — unchanged by this re-review. Cleanup commit itself adds none. |
| 7. Spec-implementation comparison | Clean | 0 | Cleanup commit does not alter any Phase 1-4 contract signature/docstring/behavior — `build_row_state_events_sql`, `iter_stream_events`, `build_topic_set`, `StreamEvent`, `build_elected_identity_index`, `elect_after_image_columns`, `generate_stream_init_config` all unchanged by it; it is purely an internal DRY refactor + test additions. `state.yaml` phase table shows all 4 phases complete. |
| 8. Workspace check | Clean | 0 | `git status --porcelain` empty. |
| 9. Pre-commit compliance | Clean | 0 | `pre-commit run --files <79 sprint-changed files>` and a separate `pre-commit run --all-files` both green (trim whitespace, end-of-file, check-yaml, check-toml, ruff, ruff-format, mypy strict, understand-bundles). |
| 10. Demo verification | Clean | 0 | All 4 demos (`phase_1_fold_split.py` … `phase_4_init_streaming.py`) run twice each, exit 0 both times, byte-identical stdout both runs. |

## Findings

None outstanding. Both gates flagged by the prior review are independently confirmed clean:

- **Gate 2 (DRY):** the three duplicated helper pairs are consolidated into `routing.py`'s `known_records_kinds` / `kind_reference_targets` / `membership_reference_fields`, imported by both `engine.py` and `init.py`; `known_records_kinds` now reads the sidecar's structured `TableSpec.category`/`record_kind` fields rather than string-slicing table names, matching the codebase's established `source/plan.py` / `source/init.py` convention. No new duplication was introduced.
- **Gate 5 (coverage):** all four previously-flagged untested branches now carry real, positive-path passing tests (sub-typed membership owner election, sub-typed-target reference translation, the `record_index` election path through the Debezium schema builder, and the membership member-field degrade branch of the init keys self-gate) — verified independently by reading each new test's assertions and by re-running targeted coverage, not by trusting the cleanup commit's own claim.

Full suite: 4314 passed, 18 skipped (matches the cleanup commit's own report). No config-boundary violations found anywhere in the sprint diff, including the cleanup commit — the consolidated helpers read exclusively from the sidecar (a sourced value, never invented), and no author-configurable value gained a default or silent fallback.

## Recommendation
- APPROVED
