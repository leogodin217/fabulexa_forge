seed: 3
operations:
  - kind: mutate_cells
    name: typo_active_actor_name
    target:
      table: records__actor
      columns: [prop__name]
    amount: { rate: 1.0 }
    mutation: { kind: typo }
    placement:
      kind: correlated
      column: prop__status
      value: active
      weight: 5.0
