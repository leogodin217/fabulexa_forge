streams:
  # Two declared streams, one per staff_type sub-type.
  nurse:
    event_count: 1
    contains_events:
      - seq: 1
        op: c
        kind: staff
        record_id: s001
        after.name: Nora Vega
  physician:
    event_count: 1
    contains_events:
      # global seq continues across topics — physician's `c` is seq 2, not reset to 1
      - seq: 2
        op: c
        kind: staff
        record_id: s002
        after.name: Owen Reed
